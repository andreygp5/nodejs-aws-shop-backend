{
  "name": "import-service",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "watch": "cdk watch",
    "test": "jest --roots import-service/src --config ../jest.config.js",
    "deploy": "cdk bootstrap && cdk deploy"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.126",
    "aws-cdk": "2.106.0",
    "ts-node": "^10.9.1",
    "typescript": "~5.2.2"
  },
  "dependencies": {
    "aws-cdk-lib": "2.106.0",
    "aws-lambda": "^1.0.7",
    "constructs": "^10.0.0",
    "dotenv": "^16.3.1"
  }
}
